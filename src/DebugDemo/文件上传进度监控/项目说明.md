## 📋 项目说明

这是一个完整的文件上传进度监控演示项目，展示了如何实现实时的文件上传进度跟踪。

## 📁 项目结构

```
文件上传进度监控/
├── index.html          # 前端页面
├── index.js            # Node.js 服务端
├── package.json        # 项目依赖配置
├── start.sh            # 快速启动脚本
├── README.md           # 详细文档
├── .gitignore          # Git 忽略配置
├── test-file.txt       # 测试文件
├── uploads/            # 上传文件存储目录
└── node_modules/       # 依赖包（自动生成）
```

## 🚀 快速启动

### 方式 1：使用启动脚本（推荐）

```bash
./start.sh
```

### 方式 2：手动启动

```bash
npm install
node index.js
```

### 方式 3：使用 npm scripts

```bash
npm start
```

然后在浏览器中打开：http://localhost:3000

## 🎯 核心实现

### 前端核心代码

**1. XMLHttpRequest 上传**

```javascript
const xhr = new XMLHttpRequest()

// 监听上传进度
xhr.upload.addEventListener('progress', (e) => {
  if (e.lengthComputable) {
    const percent = (e.loaded / e.total) * 100
    updateProgress(percent, e.loaded, e.total)
  }
})

// 发送文件
const formData = new FormData()
formData.append('file', file)
xhr.send(formData)
```

**2. 拖拽上传**

```javascript
uploadArea.addEventListener('drop', (e) => {
  e.preventDefault()
  const files = e.dataTransfer.files
  if (files.length > 0) {
    handleFileSelect(files[0])
  }
})
```

**3. 进度计算**

```javascript
function updateProgress(percent, loaded, total) {
  // 更新进度条
  progressBar.style.width = percent + '%'

  // 计算速度
  const speed = (loaded - lastLoaded) / timeDiff

  // 计算剩余时间
  const remaining = (total - loaded) / speed
}
```

### 后端核心代码

**1. Multer 配置**

```javascript
const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, 'uploads/')
  },
  filename: (req, file, cb) => {
    const uniqueName = Date.now() + '-' + file.originalname
    cb(null, uniqueName)
  },
})

const upload = multer({
  storage: storage,
  limits: { fileSize: 100 * 1024 * 1024 }, // 100MB
})
```

**2. 上传接口**

```javascript
app.post('/upload', upload.single('file'), (req, res) => {
  if (!req.file) {
    return res.status(400).json({ error: '没有文件上传' })
  }

  res.json({
    success: true,
    message: '文件上传成功',
    file: {
      filename: req.file.filename,
      originalname: req.file.originalname,
      size: req.file.size,
      path: req.file.path,
    },
  })
})
```

## 💡 关键技术点

### 1. 进度监控

- 使用 `XMLHttpRequest.upload.onprogress` 事件
- 通过 `e.loaded` 和 `e.total` 计算进度百分比

### 2. 速度计算

- 记录时间间隔内的上传数据量
- 公式：速度 = (当前已上传 - 上次已上传) / 时间间隔

### 3. 剩余时间计算

- 公式：剩余时间 = (总大小 - 已上传) / 当前速度

### 4. 文件大小格式化

```javascript
function formatFileSize(bytes) {
  if (bytes === 0) return '0 B'
  const k = 1024
  const sizes = ['B', 'KB', 'MB', 'GB']
  const i = Math.floor(Math.log(bytes) / Math.log(k))
  return (bytes / Math.pow(k, i)).toFixed(2) + ' ' + sizes[i]
}
```

## 🔧 扩展功能

可以继续添加的功能：

- [ ] 多文件同时上传
- [ ] 暂停/恢复上传
- [ ] 断点续传
- [ ] 分片上传（大文件）
- [ ] 文件类型限制
- [ ] 缩略图预览
- [ ] 上传历史记录
- [ ] WebSocket 实时通信
- [ ] 文件压缩
- [ ] 云存储集成

## 📊 性能优化

1. **大文件处理**：使用分片上传
2. **并发控制**：限制同时上传的文件数量
3. **内存优化**：使用流式处理而非一次性加载
4. **缓存策略**：合理设置静态资源缓存

## 🐛 常见问题

### 1. 上传失败

- 检查文件大小是否超过限制（100MB）
- 确认服务器是否正在运行
- 查看浏览器控制台错误信息

### 2. 进度不更新

- 确保使用 XMLHttpRequest 而非 fetch
- 检查 `upload.onprogress` 事件是否正确绑定

### 3. 跨域问题

- 确保后端已配置 CORS
- 检查请求地址是否正确

## 📝 测试步骤

1. 启动服务器：`./start.sh` 或 `node index.js`
2. 打开浏览器：http://localhost:3000
3. 选择测试文件：`test-file.txt`
4. 点击"开始上传"
5. 观察进度条、速度、剩余时间的变化
6. 上传成功后查看 `uploads/` 目录

## 🎨 UI 特性

- 渐变紫色主题
- 流畅的动画效果
- 拖拽区域高亮
- 进度条动画
- 响应式设计
- 友好的错误提示

## 📖 学习要点

通过这个项目，你将学会：

1. 如何使用 XMLHttpRequest 实现文件上传
2. 如何监听和显示上传进度
3. 如何实现拖拽上传功能
4. 如何计算上传速度和剩余时间
5. 如何使用 Multer 处理文件上传
6. 如何配置 CORS 和静态文件服务
7. 如何优雅地处理错误和异常

## 🔗 相关资源

- [MDN - XMLHttpRequest](https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequest)
- [MDN - 拖放 API](https://developer.mozilla.org/zh-CN/docs/Web/API/HTML_Drag_and_Drop_API)
- [Express.js 文档](https://expressjs.com/)
- [Multer 文档](https://github.com/expressjs/multer)

## 👨‍💻 作者

创建日期：2025 年 11 月 4 日
